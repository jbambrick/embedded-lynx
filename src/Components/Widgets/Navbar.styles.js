import styled from "styled-components";
import { Toolbar } from "@mui/material";

export const ToolBarz = styled.nav`
height: 80px;
display: flex;
justify-content: space-between;
padding: 0.5rem calc((100vw - 1000px) / 2);
z-index: 10;
text-align: center;
position: fixed;
}
`
